FROM node:20-bookworm-slim

WORKDIR /app

COPY SkillXChange-Backend/SkillXChange-User-Service/package*.json ./
RUN npm ci

COPY SkillXChange-Backend/SkillXChange-User-Service/ ./
RUN npx prisma generate

ENV NODE_ENV=production

CMD ["sh", "-c", "npx prisma migrate deploy && node index.js"]
